import { SPBody } from "../organisms";

function StartPage() {
    const className = "";

    return (
        <div className={className}>
            <SPBody />
        </div>
    );
}

export default StartPage;
