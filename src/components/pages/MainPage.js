import { NavBar } from "../molecules";

function MainPage() {
    const className = "";

    return (
        <div className={className}>
            <NavBar />
        </div>
    );
}

export default MainPage;
