import { NavBar } from "../molecules";

function FAQPage() {
    const className = "";

    return (
        <div className={className}>
            <NavBar />
        </div>
    );
}

export default FAQPage;
